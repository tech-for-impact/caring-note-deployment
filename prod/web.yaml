apiVersion: apps/v1
kind: Deployment
metadata:
  name: caring-note-web
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: caring-note-web
  template:
    metadata:
      labels:
        app: caring-note-web
    spec:
      imagePullSecrets:
        - name: kcr-secret
      containers:
        - name: caring-note-web
          image: medi-bird.kr-central-2.kcr.dev/cn-web-prod/cn-web-prod:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: caring-note-web
  namespace: default
spec:
  selector:
    app: caring-note-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
